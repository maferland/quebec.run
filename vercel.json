{
  "buildCommand": "DATABASE_URL=\"$DATABASE_URL_UNPOOLED\" prisma migrate deploy && next build",
  "crons": [
    {
      "path": "/api/cron/materialize-events",
      "schedule": "0 2 * * 0"
    }
  ]
}
